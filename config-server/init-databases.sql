-- NightFlow Database Initialization
-- This script runs on first container startup

-- Create venue service database
CREATE DATABASE nightflow_venue;

-- Create ticket service database (for future use)
CREATE DATABASE nightflow_ticket;

-- Create order service database (for future use)
CREATE DATABASE nightflow_order;
